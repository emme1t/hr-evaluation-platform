# 项目状态与来源

整理日期：2026-09-15。

## 来源

从正式 Django 工作树复制当前文件，基于 `codex/hr-evaluation-cloud` 的 `a66007f`，同时包含整理时尚未提交的离线开发文件。新仓库从独立初始提交开始；原工作树及其历史保留。

`SOURCE_FILES.json` 记录复制的文件列表。复制时逐文件 SHA-256 校验一致；随后更新根目录忽略规则及两份 README，并添加本目录说明。

## 恢复状态

- Task 9：迁移路径、500 人规模、重算一致性的 portable/SQLite 门禁完成。
- Task 10：通知队列、通道、重试与备用登录链接的 portable/SQLite 恢复完成。
- Task 11：模板保真报表、原始表单包和异常导出的 portable/SQLite 恢复完成。
- Task 12：Dashboard、角色权限、审计不可变性与通知审计边界的 portable/SQLite 恢复完成。
- Task 12 历史全量验证：792 passed / 13 PostgreSQL-only skipped。历史数字对应当时版本。
- 离线配置已提交过；离线初始化、恢复和健康检查等开发内容在本次快照中一起保存，仍需完成验证。

## 待交付门禁

真实 PostgreSQL 的锁、权限和触发器验证；正式浏览器业务与角色旅程；消息服务商验证；离线打包、备份恢复与升级验证；最终人工验收。Task 13 的事故前实际实现证据仍有缺口。

## 本次整理验证

236 个来源文件完成逐文件 SHA-256 复制校验；198 个 Python 文件通过 AST 语法检查。敏感信息模式扫描唯一命中为 test_settings_boundaries.py 中用于拒绝含凭据 URL 的虚构测试值，已人工核实。数据库附属文件、凭据、上传、备份、依赖和构建目录的 Git 忽略规则已验证，.env.example 保留跟踪。Git 差异检查通过。本次未运行完整业务测试或人工验收。
